export default class Object{
    constructor(options){
        this.position = options.statPos;
        this.speed = options.speed;
        this.hitboxOffset = options.hitboxOffset;
    }

    
    
}